# Configuración de Base de Datos PostgreSQL
# Copia este archivo a .env y ajusta los valores según tu configuración

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_contraseña_aqui
DB_NAME=taskflow
DB_SYNC=true

# Puerto del servidor (opcional)
PORT=3000

# Notas:
# - DB_SYNC=true permite que TypeORM cree automáticamente las tablas en desarrollo
# - En producción, cambia DB_SYNC=false y usa migraciones
# - Asegúrate de tener PostgreSQL instalado y corriendo
# - Crea la base de datos 'taskflow' antes de ejecutar la aplicación
